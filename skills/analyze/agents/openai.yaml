interface:
  display_name: "Analyze Implementation"
  short_description: "Read-only review of an implementation for security, performance, maintainability, and edge cases, reported to a markdown file"
  default_prompt: "Use $analyze to review the implementation across security, performance, maintainability, and edge cases, and write the findings to a markdown report."
policy:
  allow_implicit_invocation: false
